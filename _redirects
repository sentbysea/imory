# Cloudflare Pages SPA fallback.
# 실제 정적 파일/디렉터리(예: /admin/, /auth/, /onboarding/,
# /images/..., /core/...)는 이 규칙보다 먼저 매칭되어 그대로
# 서빙된다 — 이 규칙은 그 어떤 정적 파일에도 안 걸리는 경로
# (/:slug, /:slug/post/:id, /:slug/category/:id 등)에만 적용되어
# index.html을 200(리다이렉트가 아니라 rewrite)으로 돌려준다.
# 그래야 새로고침/직접 진입에서도 404가 나지 않고, URL 자체는
# 그대로 유지된 채(주소창이 안 바뀜) 클라이언트 라우팅
# (core/lib/site-path.js, posts/editor/posts-router-init.js)이
# window.location.pathname을 읽어 처리한다.
/*    /index.html   200
