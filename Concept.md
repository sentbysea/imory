# Imory Concept

> 관련 문서: [Design.md](./Design.md) · [ToDo.md](./ToDo.md)
>
> 이 문서는 2026-09-02 기준 저장소(`main` 브랜치, 최신 커밋 `906dcc1`)를 직접 조사해 작성했다. 표시 기준: **사실**(코드에서 확인됨) / **계획**(사용자와 합의된 방향, 아직 미구현) / **확인 필요**(코드·문서만으로 결론 낼 수 없음).

---

## 1. 프로젝트 한 줄 설명

Imory는 사용자가 자신만의 개인 홈페이지를 꾸미고, 글과 기록을 카테고리별로 보관하며, 마음에 드는 문장을 Quote Preset으로 꾸며 이미지로 내보낼 수 있는 **개인 홈페이지 + 기록 아카이브** 서비스다.

## 2. 프로젝트가 해결하려는 문제

- 기억하고 싶은 글·문장·기록이 여러 메모/앱에 흩어져 있고, 보기 좋게 정리하거나 공유하기 어렵다.
- 개인화(꾸미기)와 정돈된 아카이빙을 동시에 만족시키는 서비스가 마땅치 않다.
- 마음에 드는 문장을 예쁜 이미지로 만들어 공유하려면 별도 디자인 툴이 필요하다.

## 3. 주요 사용자

- 자신의 기록을 카테고리별로 정리·보관하고 싶은 개인 사용자
- 문장/대화를 꾸며 이미지로 공유하고 싶은 사용자
- 자신만의 주소를 가진 개인 홈페이지를 갖고 싶은 사용자

**사실**: 현재 신규 가입 경로는 열려 있지만(`app_config.signup_open`), 실질적으로는 1개 계정(sua, `@sentbys_a`)만 정식 운영 중인 초기 단계다.

## 4. 핵심 사용자 경험

1. Google 계정으로 로그인
2. (신규 사용자) 닉네임/슬러그를 정해 온보딩 완료 → `imory.me/<slug>` 주소 획득
3. 글을 카테고리별로 작성·조회, 필요하면 비밀글로 보호
4. 마음에 드는 문장을 Quote Preset으로 꾸며 이미지로 내보내기(다운로드/클립보드/공유)
5. **(계획)** 홈 커스터마이징 에디터로 자신의 홈페이지 레이아웃을 블록 단위로 직접 편집

## 5. 핵심 기능

| 기능 | 상태 |
|---|---|
| Google 로그인 / 세션 관리 | 코드상 구현 완료 |
| 온보딩(닉네임·슬러그 설정) | 코드상 구현 완료 |
| 글 작성/조회/수정/삭제, 카테고리 분류 | 코드상 구현 완료 |
| 비밀글(비밀번호 보호) | 코드상 구현 완료 |
| Quote Preset(CRUD, 실시간 프리뷰, "사용 중" 지정) | 코드상 구현 완료 |
| 발췌 이미지 내보내기(캡처/다운로드/클립보드/공유) | 코드상 구현 완료 |
| 카테고리 / 배너 카테고리(친구 링크 모음) | 코드상 구현 완료 |
| Settings(파비콘·커서·BGM·블로그 타이틀·MY BANNER 정보 저장) | 코드상 구현 완료 |
| 홈 커스터마이징(블록 에디터) | 일부 구현 (에디터·미리보기는 있으나 저장 기능 비활성화) |
| 신규 가입 기간 자동 제한 | 계획됨(현재는 신규 `auth.users` 생성 자체를 막는 서버 장치가 없음 — 11번 참고) |
| 공개 시작 페이지(로고·라이트/다크 전환·SIGN IN) | 계획됨 |
| 다크 모드 | 계획됨 |

> "코드상 구현 완료"는 프론트→DB/RPC 경로가 코드 추적으로 확인되었다는 뜻이며, 사용자가 실제 화면에서 검증했다는 뜻은 아니다. 검증 수준 구분은 [ToDo.md](./ToDo.md) 5-1 참고.

## 6. 서비스의 공개 영역과 로그인 영역

- **공개 영역**(비로그인 접근 가능): `/`(개발 중 placeholder 랜딩), `/:slug`(사용자별 공개 홈페이지), `/:slug/post/:id`, `/:slug/category/:id`
- **로그인 영역**: `/auth/`(OAuth callback 처리를 위한 최소 상태 화면 — 일반적인 로그인 폼 화면은 아니며, 처리 후 `/admin/`·`/onboarding/`·가입 안내로 자동 이동), `/onboarding/`(최초 1회), `/admin/`(자신의 글·Quote Preset·Settings·홈 커스터마이징 관리)

## 7. 사용자별 개인 홈페이지 개념

**사실**: `profiles.slug`가 각 사용자의 공개 주소(`imory.me/<slug>`)를 결정한다. 공개 홈페이지가 보여주는 데이터(카테고리, 글, 사이트 메타, BGM 등)를 조회하는 프론트엔드 코드는 slug → `user_id` 매핑 후 `.eq("user_id", ...)` 필터를 일관되게 사용해 사용자별로 스코핑되어 있다.

다만 **클라이언트 쿼리 필터 자체는 보안 장벽이 아니다** — 실제 데이터 격리는 DB의 RLS(Row Level Security)가 담당한다. `profiles`/`app_config`/`home_customize`/`posts`는 RLS와 column-level 권한이 migration으로 확인되었지만, `categories`/`banners`/`quote_presets`/`site_settings`/`post_contents`/`daily_visits`는 migration 파일이 없어 저장소만으로는 RLS 정책을 확인할 수 없다(**확인 필요**, Supabase Dashboard에서 직접 점검 필요 — [ToDo.md](./ToDo.md) 26번 모듈·5-6 참고).

**사실**: 홈페이지의 "테마(레이아웃·디자인)" 자체는 현재 모든 사용자에게 동일한 `themes/sua/sua-home.html`(계정 `@sentbys_a`의 프로필명·소개문이 하드코딩된 마크업)이 그대로 렌더링된다. `profiles.home_mode` 컬럼(`'customize' | 'legacy_sua'`)이 있지만 렌더러가 이 값을 아직 확인하지 않는다. 이는 [ToDo.md](./ToDo.md)의 "출시 전 필수" 항목(`[!]`)으로 등록되어 있다.

## 8. 기록·글·Quote Preset·발췌 기능의 관계

1. **글(posts)**이 가장 기본 단위이며 카테고리에 속한다.
2. 글 안의 문장에 **Quote Preset**(저장된 스타일 프리셋: 폰트·색·배경·비율 등)을 적용하면 **발췌 프리뷰**가 만들어진다.
3. 발췌 프리뷰는 html2canvas로 캡처되어 **이미지**로 내보내기(다운로드/클립보드 복사/OS 공유시트) 가능하다.

즉 순서: 글 작성 → 카테고리 분류 → (선택) Quote Preset 적용 발췌 → 이미지 내보내기.

## 9. 홈 커스터마이징 개념

**사실**: `customize/editor/`가 블록(텍스트/이미지/컨테이너/버튼/스페이서/구분선/컬럼, 최대 depth 3) 기반 비주얼 에디터를 제공하고, `preview-frame.html`이 실제 device 해상도로 미리보기를 렌더링한다. 저장 대상은 `home_customize.layout_json`(사용자당 1행)이며, 온보딩 완료 시 기본 레이아웃이 자동 생성된다.

**사실**: 에디터의 SAVE 버튼은 현재 `disabled` 상태이며 `title="저장은 다음 단계에서 구현됩니다"`라고 명시되어 있다. 즉 **편집 결과를 실제로 저장·재로드하는 기능은 아직 없다** — "일부 구현".

**계획**: 사용자 결정에 따라, 저장 기능이 완성되면 이 시스템이 **신규 가입자의 기본 홈**(`home_mode='customize'`)이 된다.

## 10. 멀티유저 운영 원칙

- **사실 / 확인 필요**: 데이터 조회는 프론트엔드 코드 레벨에서 slug → `user_id` 스코핑으로 분리되어 있다. 다만 이것이 DB 레벨(RLS)에서도 모든 테이블에 걸쳐 완전히 보장되는지는 아직 확인되지 않았다 — 자세한 내용은 7번, [ToDo.md](./ToDo.md) 26번 참고.
- **계획(확정)**: 기존 sua 계정(`@sentbys_a`)은 `home_mode='legacy_sua'`인 **레거시 전용 테마**로 계속 보존한다. 신규 사용자는 기본적으로 `home_mode='customize'`(DB 컬럼 기본값도 이미 `'customize'`, `complete_onboarding()` RPC도 신규 프로필에 명시적으로 `'customize'`를 넣는다 — 사실). 다른 사용자가 legacy_sua를 공용 템플릿처럼 선택하는 기능은 이번 범위에 포함하지 않는다.
- **계획(확정)**: `home_mode`는 현재 단계에서 사용자가 직접 고르는 Settings 토글이 아니라, 시스템이 내부적으로 참조하는 값으로만 쓴다. 사용자 노출 UI는 여러 기본 테마·초기화 기능이 필요해질 때 별도로 재검토한다.
- 관리자라는 별도 역할/계정은 없다 — 로그인한 사용자가 곧 자기 데이터의 관리자다(사실, `admin/admin-session.js` 확인).

## 11. 신규 가입과 기존 회원 로그인의 차이

Imory에 "가입"이 이뤄지는 과정은 실제로 3단계로 구성된다. 이 구분이 중요한 이유는, 지금까지 이 문서를 포함한 계획들이 2~3단계만 막고 있으면서도 이를 "신규 가입 차단"이라고 불러왔기 때문이다.

1. **Supabase Auth 사용자 생성** — Google OAuth 인증에 성공하면 Imory `profiles`가 생기기 전에 먼저 Supabase `auth.users`에 계정이 만들어진다.
2. **Imory profiles 생성 및 온보딩 완료** — `complete_onboarding()` RPC로 닉네임·슬러그를 확정해야 실제 Imory 사용자가 된다.
3. **Imory 서비스 이용** — `profiles`가 있으면 이후 가입 기간과 무관하게 계속 로그인·이용 가능.

**사실**: 현재 `auth/auth-callback.js`의 `signup_open` 검사는 **2~3단계만** 제한한다(온보딩 화면 진입을 막을 뿐). **1단계(신규 `auth.users` 생성) 자체를 서버 측에서 막는 장치는 현재 코드에 없다** — 즉 지금 상태를 "신규 가입 완전 차단"이라고 표현할 수 없다. 가입이 닫혀 있어도 `auth.users`에는 계정이 생성되고, `profiles`만 안 생긴 상태로 남을 수 있다.

- **기존 회원**: `profiles` row 존재 → 가입 기간과 무관하게 항상 `/admin/`으로 로그인 가능(사실).
- **신규 가입(현재)**: `profiles` row 없음 + `app_config.signup_open = true`일 때만 온보딩 진행 가능(사실, 단 위에서 설명한 대로 2~3단계 한정). `signup_opens_at`/`signup_closes_at`(둘 다 `timestamptz`)는 DB에 있지만 코드에서 아직 참조되지 않는다(사실).

**계획(확정)** — 가입 제한을 다음 3중 방어로 구현한다:

1. **1차 — Before User Created Hook**: Supabase의 Before User Created Hook으로 신규 `auth.users` 생성 직전에 `signup_open` + `signup_opens_at`/`signup_closes_at`(서버 시각 기준)을 검사해, 조건을 만족하지 않으면 계정 생성 자체를 거절한다. 기존 사용자의 로그인은 이 Hook의 영향을 받지 않아야 한다.
2. **2차 — OAuth callback**: `auth-callback.js`가 `profiles` 존재 여부와 가입 상태를 확인해 기존 회원은 `/admin/`, 신규+가입가능은 `/onboarding/`, 신규+가입불가는 안내 화면으로 분기한다. 이 검사는 **UX·화면 이동을 위한 보조 검사**이며 최종 보안 장벽이 아니다.
3. **3차 — complete_onboarding() RPC**: RPC 내부에서도 서버 시각 기준으로 가입 가능 여부를 다시 검사해, Hook 설정 누락·callback 우회·RPC 직접 호출 상황에서도 가입이 닫혀 있으면 `profiles`가 생성되지 않도록 한다.

가입 기간 표시/저장 원칙(계획): DB에는 UTC로 저장, 운영자 입력·사용자 표시 기준은 Asia/Seoul(KST), 판정은 서버 시각과 `timestamptz` 비교로 하며 브라우저 로컬 시각만으로 허용 여부를 결정하지 않는다.

기간 한정 가입의 완료 조건은 **Hook + callback + RPC 3중 검사가 모두 구현되었을 때**로 본다 — 자세한 단계는 [ToDo.md](./ToDo.md) 10번 모듈·5-5 참고.

## 12. 현재 구현된 범위

5번 표의 "코드상 구현 완료" 항목 전체(Google 로그인, 온보딩, 글/카테고리/비밀글, Quote Preset, 발췌 이미지 내보내기, 배너, Settings 저장) — 이는 프론트→DB/RPC 경로가 코드 추적으로 확인되었다는 뜻이며, 사용자가 실제 화면에서 검증했다는 뜻으로 확대 해석하지 않는다. 상세 검증 상태(코드/로컬 화면/Supabase 연동/배포 환경/사용자 확인 5단계 구분)는 [ToDo.md](./ToDo.md) 5-1·5-3 참고.

## 13. 앞으로 구현할 범위

우선순위 순서(자세한 단계는 [ToDo.md](./ToDo.md) 5-5 참고, 총 0~5단계·하위 단계 포함 10개 작업 단위):

1. **(출시 전 필수)** `home_mode` 기반 렌더러 분기 — 모든 사용자에게 동일한 sua 마크업이 노출되는 문제 해결
2. 공개 시작 페이지 기본 헤더 + `imory` 로고(시작 페이지 한정, 텍스트+CSS)
3. 다크 모드 시스템 토큰(2A) + 테마 전환 버튼/로직(2B)
4. SIGN IN 버튼과 기존 Google OAuth 진입 연결
5. 가입 제어 3중 방어 — 서버 측 판정 함수(4A) + Before User Created Hook 연결(4B) + `complete_onboarding()` 재검사(4C) + callback 화면 분기(4D)
6. 데스크톱·모바일·기존/신규 사용자 통합 검증
7. home_customize 저장/불러오기 연결
8. (후속) 다른 화면(auth/onboarding/admin 등)으로 로고를 확장할지 검토 — 시작 페이지 디자인이 확정되고 사용자가 확인한 뒤 별도 진행
9. (보류) MY BANNER 공유 UI

## 14. 이번 프로젝트에서 하지 않을 것

- React/Vue/Next.js 등 프레임워크 전환
- Node.js 등 별도 백엔드 서버 신설
- MariaDB 등 다른 DB 도입
- 하드코딩된 관리자 계정(`admin/12345` 등) — 현재도 없고 앞으로도 만들지 않는다
- MY BANNER를 공개 홈페이지에 자동 노출하는 것(계획, Q3 결정 — MY BANNER는 "다른 사이트 운영자가 나를 링크할 때 쓰는 배너 정보"이지 내 홈에 자동으로 뜨는 배너가 아니다)
- legacy_sua를 다른 사용자가 선택 가능한 공용 템플릿으로 만드는 것

## 15. 비기능 요구사항

- **모바일 지원**: 다수 화면에 모바일 전용 CSS/바텀시트 존재(`posts-mobile.css`, `admin-quote-mobile-tabs.js` 등) — 화면별 완성도는 [Design.md](./Design.md) 4-9 참고, 전체 일관성은 확인 필요
- **접근성**: 일부 컴포넌트(`imory-field`, `imory-tab`)에 focus-visible 규칙 존재, 전면 점검은 안 됨 — 확인 필요
- **성능**: 별도 측정/최적화 기록 없음 — 확인 필요
- **보안**: RLS + column-level GRANT + SECURITY DEFINER RPC 조합으로 진행 중, 미해결 항목은 ToDo.md "출시 전 필수" 섹션 참고
- **사용자별 데이터 분리**:
  - 애플리케이션 쿼리 스코핑: 코드상 구현됨(프론트엔드가 `user_id` 필터를 일관되게 사용, 7번 참고)
  - DB 레벨 RLS 격리: 일부 확인됨(`profiles`/`app_config`/`home_customize`/`posts`), 일부 확인 필요(`categories`/`banners`/`quote_presets`/`site_settings`/`post_contents`/`daily_visits` — [ToDo.md](./ToDo.md) 26번)
  - 홈 렌더링 격리: 미완료 — `legacy_sua` 분기 미구현으로 모든 사용자가 동일한 sua 마크업을 봄(7번, [ToDo.md](./ToDo.md) 12번 `[!]`)
- **기존 데이터 보호**: 이번 문서화 세션에서 기존 코드·DB·데이터를 변경하지 않았다

## 16. 프로젝트 용어 정리

| 용어 | 뜻 |
|---|---|
| slug | 사용자별 공개 주소 식별자(`imory.me/<slug>`) |
| owner | 특정 slug/URL이 가리키는 데이터 소유 사용자 |
| home_mode | 사용자 공개 홈페이지 렌더링 방식(`customize` \| `legacy_sua`) |
| Quote Preset | 발췌 이미지의 스타일(폰트·색·배경·비율 등) 저장 프리셋 |
| 발췌 프리뷰 | Quote Preset이 적용된, 이미지로 내보내기 전 미리보기 화면 |
| 온보딩 | 최초 로그인 후 닉네임·슬러그를 설정해 프로필을 생성하는 1회성 절차 |
| legacy_sua | 기존 계정(`@sentbys_a`)의 홈페이지를 보존하기 위한 전용 테마 |
| 배너 카테고리 | 글 목록 대신 링크 그리드를 보여주는 카테고리 종류(`categories.type='banner'`) |
| MY BANNER | 다른 사이트 운영자가 내 사이트를 링크할 때 쓸 수 있도록 저장해두는 배너 이미지/URL(내 공개 홈에 자동 노출되지 않음) |
